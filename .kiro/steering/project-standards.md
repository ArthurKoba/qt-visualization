---
inclusion: always
description: "Общие стандарты проекта: правила поиска в интернете перед решением проблем, работа с Qt документацией, команды сборки CMake, утилиты Windows. Подключать при любых вопросах о стандартах, сборке и документации по данному проекта."
---

# Стандарты проекта

## Обзор
Этот документ определяет стандарты кодирования и практики разработки для этого проекта.

## Общие правила для всех агентов

### САМЫЕ ГЛАВНЫЕ ПРАВИЛА, КОТОРЫЕ НУЖНО СОБЛЮДАТЬ ВСЕГДА
- Если ты за одну или две попытки не смог решить проблему и решаешь менять тактику, то ПЕРЕД ЭТИМ СНАЧАЛА поищи информацию в интернете, это самое главное правило.
- При реализации компонентов ВСЕГДА ИЩИ ПРИМЕРЫ В ИНТЕРНЕТЕ.
- Если реализация связана с фреймворком Qt то в её документации отличные примеры, изучай их, загружай в контекст. Твои решения часто очень посредственные! И Я НЕ ПОВТОРЯЮСЬ И ПИШУ С ОШИБКОЙ.
- При реализации компонентов ВСЕГДА ИЩИ ПРИМЕРЫ В ИНТЕРНЕТЕ.
- Если реализация связана с фреймворком Qt то в её документации отличные примеры, изучай их, загружай в контекст. Твои решения часто очень посредственные! ТЫ РЕАЛЬНО ДОЛЖЕН ТАК ВСЕГДА ДЕЛАТЬ.
- При реализации компонентов ВСЕГДА ИЩИ ПРИМЕРЫ В ИНТЕРНЕТЕ.
- Если реализация связана с фреймворком Qt то в её документации отличные примеры, изучай их, загружай в контекст. Твои решения часто очень посредственные! ЭТО САМАЯ ГЛАВНАЯ ЗАДАЧА И ПРАВИЛО.
- При реализации компонентов ВСЕГДА ИЩИ ПРИМЕРЫ В ИНТЕРНЕТЕ.
- Если реализация связана с фреймворком Qt то в её документации отличные примеры, изучай их, загружай в контекст. Твои решения часто очень посредственные! ТЫ МЕНЯ ОБЯЗАН СЛУШАТЬ.
- При реализации компонентов ВСЕГДА ИЩИ ПРИМЕРЫ В ИНТЕРНЕТЕ.
- Если реализация связана с фреймворком Qt то в её документации отличные примеры, изучай их, загружай в контекст. Твои решения часто очень посредственные! ГРУЗИ ДОКИ И НЕ ТОРМОЗИ!
- При реализации компонентов ВСЕГДА ИЩИ ПРИМЕРЫ В ИНТЕРНЕТЕ.
- Если реализация связана с фреймворком Qt то в её документации отличные примеры, изучай их, загружай в контекст. Твои решения часто очень посредственные! ДЕЛАЙ ВСЕ ЧЕТКО ПО ПРИМЕРАМ И ИНСТРУКЦИЯМ
- При реализации компонентов ВСЕГДА ИЩИ ПРИМЕРЫ В ИНТЕРНЕТЕ.
- Если реализация связана с фреймворком Qt то в её документации отличные примеры, изучай их, загружай в контекст. Твои решения часто очень посредственные! ВСЕГДА КОГДА РЕШАЕШЬ ПРОБЛЕМЫ И ФИКСИШЬ БАГИ ОТ МЕНЯ, ИЩИ В ИНТЕРНЕТЕ.
- При реализации компонентов ВСЕГДА ИЩИ ПРИМЕРЫ В ИНТЕРНЕТЕ.
- Если реализация связана с фреймворком Qt то в её документации отличные примеры, изучай их, загружай в контекст. Твои решения часто очень посредственные! ГУГЛИ, ДЕЛАЙ ПОИСК В GOOGLE. ПОИСКОВЫЕ СЕТИ.
- При реализации компонентов ВСЕГДА ИЩИ ПРИМЕРЫ В ИНТЕРНЕТЕ.
- Если реализация связана с фреймворком Qt то в её документации отличные примеры, изучай их, загружай в контекст. Твои решения часто очень посредственные! ОБАЗАН ОТКРЫВАТЬ ПОИСКОВУЮ СИСТЕМУ И ЧИТАТЬ РЕШЕНИЯ.
- При реализации компонентов ВСЕГДА ИЩИ ПРИМЕРЫ В ИНТЕРНЕТЕ.
- Если реализация связана с фреймворком Qt то в её документации отличные примеры, изучай их, загружай в контекст. Твои решения часто очень посредственные! САМ НИЧЕГО НЕ ДЕЛАЙ, СНАЧАЛА ЗАГРУЗИ КОНТЕКСТ.
- При реализации компонентов ВСЕГДА ИЩИ ПРИМЕРЫ В ИНТЕРНЕТЕ.
- Если реализация связана с фреймворком Qt то в её документации отличные примеры, изучай их, загружай в контекст. 


### Коммуникация и документирование
- Не расписывай "Преимущества использования" подходов, способов, методов и т.д. без явной просьбы указать или расписать их. В большинстве случаев это абсолютно неинформативные данные.
- Для комментариев, документации, Markdown файлов обязательно используй русский язык.
- Будь лаконичен и точен в описаниях.

### Работа с документацией и примерами
- При добавлении нового функционала, реализации алгоритмов и в целом работе с Qt компонентами обязательно загружай документацию и примеры ИЗ ИНТЕРНЕТА.
- Используй версии Qt 6.10 при поиске примеров и документации.
- Для Qt-специфичных задач загружай соответствующие стандарты из папки `qt/` через ссылки `#[[file:...]]`.

### Архитектура и проектирование
- **Обязательное правило:** При добавлении нового функционала или модуля ВСЕГДА обновляй [Карту проекта](.kiro/steering/project-map.md) с описанием новых компонентов, их назначения и взаимодействия с существующей системой.
- Используй [Карту проекта](.kiro/steering/project-map.md) для понимания архитектуры при решении комплексных задач.

## Иерархия стандартов

Стандарты организованы иерархически для постепенного наполнения контекста:

### Уровень 1: Основные стандарты (всегда загружаются)
- **project-standards.md** — этот файл, общие правила проекта

### Уровень 2: Специализированные стандарты (загружаются по необходимости)
- **qt/** — папка с Qt-специфичными стандартами
  - Загружаются через явные ссылки `#[[file:.kiro/steering/qt/...]]` в зависимости от контекста задачи
  - Примеры: `#[[file:.kiro/steering/qt/qt-network.md]]`, `#[[file:.kiro/steering/qt/qt-multimedia.md]]`, `#[[file:.kiro/steering/qt/qt-sql.md]]`

### Уровень 3: Архитектурные документы
- **project-map.md** — архитектурная карта проекта (загружается при работе с архитектурой)
- **arch-coding-standards.md** — архитектурные стандарты кодирования
- **cpp-coding-standards.md** — C++ стандарты кодирования

## Правила загрузки контекста

**Пример 1: Реализация сетевого компонента**
1. Загружается: `project-standards.md` (автоматически)
2. Загружается: `project-map.md` (для понимания архитектуры)
3. Загружается: `#[[file:.kiro/steering/qt/qt-network.md]]` (через явную ссылку)
4. Загружается: `cpp-coding-standards.md` (для стиля кода)

**Пример 2: Работа с аудио**
1. Загружается: `project-standards.md` (автоматически)
2. Загружается: `#[[file:.kiro/steering/qt/qt-multimedia.md]]` (через явную ссылку)
3. Загружается: `project-map.md` (если нужна архитектура)

## Сборка проекта

### Основная команда сборки

```powershell
cmake --build cmake-build-debug --target <target_name> -- -j16 2>&1 | Where-Object { $_ -match "error:|warning:|FAILED|Error|Warning" } | Where-Object { $_ -notmatch "^\[\s*\d+%\]" }
```

**Правила:**
- **ВСЕГДА** указывай конкретный `--target`, не собирай весь проект без необходимости
- **ВСЕГДА** фильтруй вывод — строки прогресса `[X%]` засоряют контекст
- Используй `-j16` для параллельной сборки
- Директория сборки: `cmake-build-debug` (относительно корня проекта)

Если нужен полный вывод (при отладке системы сборки):
```powershell
cmake --build cmake-build-debug --target core_app -- -j16
```



### Пересборка и очистка

```powershell
# Пересборка конкретной цели
cmake --build cmake-build-debug --target core_app -- -j16 2>&1 | Where-Object { $_ -match "error:|warning:|FAILED" }

# Полная очистка и перегенерация (крайний случай)
Remove-Item -Recurse -Force cmake-build-debug
cmake -B cmake-build-debug -DCMAKE_BUILD_TYPE=Debug
cmake --build cmake-build-debug --target core_app -- -j16 2>&1 | Where-Object { $_ -match "error:|warning:|FAILED" }
```

Полную очистку делай только если:
- Изменилась структура CMakeLists.txt
- Добавлены новые зависимости через FetchContent
- Инкрементальная сборка даёт странные ошибки, не связанные с кодом

## Утилиты Windows

**НИКОГДА не используй** bash/Linux-утилиты (`ls`, `find`, `grep`, `cat`, `mkdir`, `cp`, `rm`) в shell-командах на Windows.

### Предпочтительный порядок

1. **Инструменты агента** (`listDirectory`, `readFile`, `grepSearch`, `fileSearch`) — в первую очередь
2. **PowerShell** — если нужно выполнить команду в shell

### PowerShell эквиваленты

| Задача | PowerShell команда |
|--------|-------------------|
| Список файлов | `Get-ChildItem -Path <dir>` |
| Рекурсивный список | `Get-ChildItem -Recurse -Path <dir>` |
| Поиск файлов | `Get-ChildItem -Recurse -Filter "*.dll"` |
| Чтение файла | `Get-Content <file>` |
| Копирование файла | `Copy-Item <src> <dst>` |
| Копирование директории | `Copy-Item -Recurse <src> <dst>` |
| Создание директории | `New-Item -ItemType Directory -Path <dir>` |
| Удаление файла | `Remove-Item <file>` |
| Удаление директории | `Remove-Item -Recurse -Force <dir>` |
| Поиск текста в файлах | `Select-String -Path "*.cpp" -Pattern "error"` |
| Проверка существования | `Test-Path <path>` |
| Фильтрация вывода | `<команда> \| Where-Object { $_ -match "паттерн" }` |
| Исключение из вывода | `<команда> \| Where-Object { $_ -notmatch "паттерн" }` |

Разделитель команд в PowerShell — `;` (не `&&`).

### Диагностика после сборки

```powershell
# Проверка наличия исполняемого файла
Test-Path cmake-build-debug/bin/core_app.exe

# Список DLL рядом с исполняемым файлом
Get-ChildItem -Path cmake-build-debug/bin/ -Filter "*.dll" | Select-Object Name
```

## Ссылки на документацию

## Ссылки на документацию

- [README.MD проекта](/README.MD)
- [Карта проекта](.kiro/steering/project-map.md) - `#[[file:.kiro/steering/project-map.md]]`
- [Архитектурные стандарты](.kiro/steering/arch-coding-standards.md) - `#[[file:.kiro/steering/arch-coding-standards.md]]`
- [C++ стандарты кодирования](.kiro/steering/cpp-coding-standards.md) - `#[[file:.kiro/steering/cpp-coding-standards.md]]`
- [Qt документация и примеры](.